#!/usr/bin/env bash
set -euo pipefail

repo_root="$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")/.." && pwd)"
cd "$repo_root"

if [[ "${AMP_ORB:-}" == "1" ]]; then
  echo "[resume] Ensuring declared orb services are healthy..."
  amp orb services ensure
fi

echo "[resume] Ready."
